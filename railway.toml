[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "cd backend && node dist/index.js"
healthcheckPath = "/trpc/articles.list"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
